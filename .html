<!-- ** CHEAT SHEET -->

<!-- **HTML 5 or ! or Doc --> used to create new html file
<!-- ( *{} )   Used to code in CSS  -->
<!-- ** margin: 0; 
    * padding: 0;
   * font-family: sans-serif;
  *  box-sizing: border-box; -->

  <!-- ! For Hover Effect -->    Example,,,
 <!-- * nav ul li a::after{
   * content: '';
   * width: 0%;
   * height: 3px;
   * background:  #ff4747;
   * position: absolute;
   * left: 0;
   * bottom: -8px;
   * transition: 0.5s; -->

<!-- ? nav ul li a:hover::after{
    ? width: 100%; -->
}
<!-- * cursor: pointer;
   * position: relative; 

   ? The position CSS property sets how an element is positioned in a document. The top, right, bottom, and left properties determine the final location of positioned elements.-->

   <!-- ! FLEX - used to place the content side by side
 ! Flex Basis - used to space btween image and content or text-->    For Example,,
 <!-- <!-- ? .aboutcol-1{                                 
   ? flex-basis: 35%;
} -->
<!-- ? .aboutcol-2 {
   ? flex-basis: 60%;
}-->
 <!-- ? .html{
  ? scroll-behaviour:smooth;
 ? }  -->